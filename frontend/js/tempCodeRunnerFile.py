 {"_index": "restaurants", "_id": str(count)}}
    # indexData = {"recID": i['recID']['S'],
    #              "cuisine": i['cuisineType']['S']}

    # print(index)
    # print(indexData)
    # print("=====================")
    # f.write(json.dumps(index))
    # f.write(json.dumps(indexData))

    # count += 1
